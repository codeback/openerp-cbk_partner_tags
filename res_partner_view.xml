<?xml version="1.0"?>
<openerp>
    <data>        
        <record id="view_partner_tags_form" model="ir.ui.view">
            <field name="name">res.partner.tags.form</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="priority" eval="20"/>
            <field name="arch" type="xml">    
                <field name="category_id" position="after">
                    <field name="tag_channel" invisible="True"/>
                    <field name="tag_distribution" invisible="True"/>
                </field>                                       
            </field> 
        </record>      

        <record model="ir.ui.view" id="view_partner_tags_kanban">
            <field name="name">res.partner.tags.kanban</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.res_partner_kanban_view"/>
            <field name="arch" type="xml">
                <field name="category_id" position="after">
                    <field name="tag_channel" invisible="True"/>
                    <field name="tag_distribution" invisible="True"/>
                </field> 
            </field>
         </record>
    </data>
</openerp>
